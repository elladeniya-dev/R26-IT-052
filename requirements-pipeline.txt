# Everything the deployed API needs (requirements.txt), PLUS the heavier
# packages only the offline ETL/ML pipeline (app/pipeline/) uses — GLiNER
# NLP, LightGBM, the H&M color/attribute matching, Gemini's optional
# refinement path. Verified none of these load when the live API starts
# (checked sys.modules directly) — installing them into the deployed web
# service would only add build time, memory, and cold-start cost for zero
# runtime benefit. Used by the GitHub Actions daily harvest workflow, not
# by `render.yaml`.
-r requirements.txt
pytest>=8.0.0
joblib==1.5.3
scikit-learn==1.6.1
scipy==1.17.1
threadpoolctl==3.6.0
torch>=2.0.0
gliner>=0.2.0
google-genai>=2.16.0
webcolors==25.10.0
lightgbm==4.7.0
