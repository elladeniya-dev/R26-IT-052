# Copy to .env and fill in real values. Never commit the real .env.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Protects POST/admin routes under /api/v1/admin — the scraper's ingest job
# authenticates with this. Generate any long random string.
ADMIN_API_KEY=""
